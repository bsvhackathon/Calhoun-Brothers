import { myPrivateKey, myAddress, myPublicKey } from './privateKey'

// Use the keys
console.log('Address:', myAddress.toString())
console.log('Private Key:', myPrivateKey.toString())
console.log('Public Key:', myPublicKey.toString())